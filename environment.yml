name: image-captioning

channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults

dependencies:
  # Python
  - python=3.10
  - pip

  # PyTorch with CUDA 11.8
  - pytorch=2.4.*
  - torchvision=0.19.*
  - torchaudio=2.4.*
  - pytorch-cuda=11.8

  # Core scientific stack
  - numpy
  - pandas
  - matplotlib
  - scipy
  - scikit-learn
  - pillow
  - tqdm
  - pyyaml

  # NLP / metrics
  - nltk

  # Jupyter / development
  - jupyterlab
  - notebook
  - ipykernel

  # pip-only packages
  - pip:
      - rouge-score
      - pycocoevalcap
